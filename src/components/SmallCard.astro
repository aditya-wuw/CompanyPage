---
import TPButton from "./Buttons/TPButton.astro";

interface Props {
  image: string;
  alt: string;
  title?: string;
}

const { image, alt, title } = Astro.props;
---

<div
  class="relative lg:w-120 lg:h-50 sm:w-90 h-40 w-75 bg-black/30 backdrop-blur-xl text-white overflow-hidden drop-shadow-2xl drop-shadow-black/20 rounded-md"
>
  <h1 class="text-2xl">{title}</h1>
  <!-- <div class="bg-black/20 backdrop-blur-xl text-white w-fit p-1 rounded-md absolute left-2 top-2">
    New !
  </div> -->
  <div class="absolute z-1 bottom-4 right-2">
    <TPButton label="Check latest" link="Assets" />
  </div>
  <img
    src={image}
    alt={alt}
    class="object-cover inset-0 absolute -z-1 w-full h-full"
  />
</div>
